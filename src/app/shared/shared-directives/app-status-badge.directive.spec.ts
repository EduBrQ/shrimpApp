import { AppStatusBadgeDirective } from './app-status-badge.directive';

describe('AppStatusBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new AppStatusBadgeDirective();
    expect(directive).toBeTruthy();
  });
});
