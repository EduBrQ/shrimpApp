import { IconAnexoDirective } from './icon-anexo.directive';

describe('IconAnexoDirective', () => {
  it('should create an instance', () => {
    const directive = new IconAnexoDirective();
    expect(directive).toBeTruthy();
  });
});
